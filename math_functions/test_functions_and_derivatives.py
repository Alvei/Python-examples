import pytest
from functions_and_derivatives import scale, raise_val

def test_scale():
    #my_func = scale(32)  # Assign the return function with n = 32
    #raise my_func(2) == 64
    #raise my_func(3) == 96
    pass
def test_raise_val():
    pass
    """# Assign the funtion and define the first outer argument - exp
    f_square = raise_val(2)
    f_cube = raise_val(3)

    raise f_square(10) == 100
    #raise f_cube(10) == 1000 """